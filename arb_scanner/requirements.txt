# Polymarket Sports Arbitrage Scanner — Dependencies
# Tested with Python 3.11+

# Core async HTTP / WS
httpx>=0.27.0
websockets>=12.0

# Configuration
PyYAML>=6.0

# Polymarket official SDK (order placement, signing)
py-clob-client>=0.20.0

# Linear programming (combinatorial arbitrage covering portfolio)
pulp>=2.7.0

# Data manipulation (backtest, snapshots)
# (stdlib only — no extra deps for core scanner)

# Optional: faster Polygon RPC
# web3>=6.0.0

# Optional: structured logging
# structlog>=24.0.0

# Optional: monitoring dashboards
# streamlit>=1.30.0
# plotly>=5.18.0
